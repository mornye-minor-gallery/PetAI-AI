# 공개 저장소 기여 기준

이 저장소는 전체 게임 저장소의 미러가 아니라, 검토를 거쳐 공개한 AI 구현과 연구의 정본이다. AI 런타임·메모리·평가 코드와 재현 가능한 설명만 반영한다. Unity 게임 프로젝트와 에셋, 제품 배포 설정, 원본 대화, 재배포 권한이 불분명한 데이터·모델은 반영하지 않는다.

비공개 개발 저장소의 브랜치나 이력을 그대로 병합하지 않는다. 필요한 변경을 선별해 이 저장소의 작업 브랜치에서 검토하고, 외부 자료의 출처와 이용 조건도 확인한다.

## 첫 push 전에

1. 추가하려는 파일, 스테이징된 차이, 새 커밋 각각의 내용을 확인한다.
2. 코드·문서·커밋 메시지·PR 제목과 본문·리뷰·댓글·첨부 로그에 개인 파일 경로, 비공개 계정 식별자, 개인 연락처, 식별 가능한 기기 주소, 실제 이용자 데이터 또는 비밀값이 없는지 확인한다. 경로는 저장소 상대경로로, 테스트 입력은 합성 데이터로 바꾼다.
3. 커밋의 작성자·커미터 이름과 이메일은 기여자가 선택한다. 실제 이메일을 공개하고 싶지 않다면 GitHub의 비공개 커밋 이메일을 사용한다. 이 클론에서 첫 push 전에 훅을 설치하고, 커밋을 마친 뒤 검사를 실행한다.

```bash
python3 scripts/install_privacy_hook.py
python3 scripts/check_repository_privacy.py --base origin/main
```

설치한 `pre-push` 훅은 같은 클론의 모든 worktree에 적용되며 새 커밋의 추가된 텍스트와 커밋 메시지에서 개인 홈 경로·이메일·전화번호를 찾는다. 작성자·커미터 정보는 검사하지 않는다. 훅이나 검사기가 바뀌면 설치기를 다시 실행한다. 기존의 다른 `pre-push` 훅은 덮어쓰지 않는다. 훅은 클론마다 설치해야 하고 우회할 수 있다. 바이너리, 모든 비밀값 유형, PR의 글과 과거 이력까지 안전하다고 증명하지는 않는다. 문제가 있으면 push 전에 커밋을 고친다. 이미 노출된 비밀값은 먼저 폐기·교체한다.

PR은 변경 범위, 재현 방법, 검사 결과, 외부 자료의 출처와 공개 가능 여부를 설명한다. `main`에 직접 push하지 않고 PR로 검토한다.
